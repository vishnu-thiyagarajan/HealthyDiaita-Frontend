import React from 'react'
import { TouchableOpacity, Text, StyleSheet, View, Button, SafeAreaView, StatusBar } from 'react-native'

export default function Home ({navigation}){
  return (
    <SafeAreaView>
      <StatusBar style="auto" />
      <Text>Home page</Text>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({})
